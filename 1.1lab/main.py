# Задание 4 из ЕГЭ-2025 профиль [https://math-ege.sdamgia.ru/test?id=86355913]
В кармане у Миши было четыре конфеты  — «Грильяж», «Белочка», «Коровка» и «Ласточка», а также ключи от квартиры. Вынимая ключи, Миша случайно выронил из кармана одну конфету. Найдите вероятность того, что потерялась конфета «Грильяж».

def calculate_probability():
    """
    Рассчет вероятности потери конфеты 'Грильяж'
    """
    # Всего конфет в кармане
    total_candies = 4
    candies = ['Грильяж', 'Белочка', 'Коровка', 'Ласточка']
    
    # Количество конфет 'Грильяж'
    grilliaz_count = 1
    
    # Вероятность = благоприятные исходы / все возможные исходы
    # Благоприятный исход: потерялась именно конфета 'Грильяж'
    favorable_outcomes = grilliaz_count
    
    # Все возможные исходы: могла потеряться любая из 4 конфет
    total_possible_outcomes = total_candies
    
    probability = favorable_outcomes / total_possible_outcomes
    
    # Проверка корректности вероятности
    assert 0 <= probability <= 1, "Вероятность должна быть между 0 и 1"
    assert favorable_outcomes <= total_possible_outcomes, "Благоприятных исходов не может быть больше всех возможных"
    
    return probability

def verify_with_classical_definition():
    """
    Проверка с использованием классического определения вероятности
    """
    # Все элементарные события равновозможны
    elementary_events = ['Грильяж', 'Белочка', 'Коровка', 'Ласточка']
    
    # Событие A - потерялась конфета 'Грильяж'
    event_A = ['Грильяж']
    
    # Вероятность по классическому определению: P(A) = m/n
    # где m - число исходов, благоприятствующих A
    # n - общее число равновозможных исходов
    m = len(event_A)
    n = len(elementary_events)
    
    return m / n

# Основные расчеты
prob = calculate_probability()
prob_classical = verify_with_classical_definition()

print("=== РЕШЕНИЕ ЗАДАЧИ ===")
print(f"Всего конфет: 4")
print(f"Конфет 'Грильяж': 1")
print(f"Благоприятные исходы: 1 (потерялась 'Грильяж')")
print(f"Все возможные исходы: 4 (могла потеряться любая конфета)")
print(f"\nВероятность: P = 1/4 = {prob}")
print(f"Проверка классическим определением: {prob_classical}")

# Дополнительная проверка через дроби
from fractions import Fraction
fraction_prob = Fraction(1, 4)
print(f"Вероятность в виде дроби: {fraction_prob}")

print(f"\nОТВЕТ: Вероятность того, что потерялась конфета 'Грильяж', равна {prob} или {fraction_prob}")
